// 2019-01-04 13:42:59
///export_font_tex(dir, grid, bg, bgcolour)
/*
    Exports font texture
*/

#args dir, rendergrid, renderbg, bgcol

// Build font texture
build_font_tex_ext(rendergrid, bgcol, renderbg);

// Save font texture
surface_save(fntTex, dir);

// Build font texture again
build_font_tex();
